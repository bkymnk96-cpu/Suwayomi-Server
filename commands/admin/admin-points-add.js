const { SlashCommandBuilder, PermissionFlagsBits } = require('discord.js');
const { success, error } = require('../../utils/embeds');
const points = require('../../utils/adminPoints');
module.exports = { data: new SlashCommandBuilder().setName('admin-points-add').setDescription('إضافة نقاط لإداري').addUserOption(o=>o.setName('member').setDescription('الإداري').setRequired(true)).addIntegerOption(o=>o.setName('amount').setDescription('عدد النقاط').setRequired(true).setMinValue(1)).addStringOption(o=>o.setName('reason').setDescription('السبب')).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild), async execute(interaction){ const cfg=points.getConfig(interaction.guildId); if(!points.hasManager(interaction.member,cfg)) return interaction.reply({embeds:[error('لا تمتلك صلاحية إدارة نقاط الإدارة')],flags:['Ephemeral']}); const user=interaction.options.getUser('member'); const amount=interaction.options.getInteger('amount'); const reason=interaction.options.getString('reason')||'إضافة يدوية'; const p=points.add(interaction.guildId,user.id,amount,reason,interaction.user.id,'manual'); return interaction.reply({embeds:[success(`تمت إضافة **${amount}** نقطة إلى ${user}. الإجمالي الآن: **${p.total}**`)]}); } };

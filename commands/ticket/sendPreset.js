const { SlashCommandBuilder, PermissionFlagsBits, ChannelType } = require('discord.js');
const db = require('../../database/db');
const designer = require('../../utils/ticketDesigner');
const { success, error } = require('../../utils/embeds');
module.exports = { data: new SlashCommandBuilder().setName('send-preset').setDescription('إرسال بانل التذاكر المحفوظ').addChannelOption(o=>o.setName('channel').setDescription('روم الإرسال').addChannelTypes(ChannelType.GuildText,ChannelType.GuildAnnouncement)).setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild), async execute(interaction){ const settings=db.getTicketSettings(interaction.guildId); if(!settings.category_id||!settings.staff_role) return interaction.reply({embeds:[error('أكمل إعداد التذاكر أولاً عبر /setup-ticket أو /ticket-setup')],flags:['Ephemeral']}); const panel=designer.getPanelData(interaction.guildId); const channel=interaction.options.getChannel('channel')||interaction.guild.channels.cache.get(settings.panel_channel)||interaction.channel; await channel.send({embeds:[designer.zeusPanelEmbed(interaction.guild,panel)],components:designer.buildTicketComponents(panel)}); return interaction.reply({embeds:[success(`تم إرسال بانل التذاكر في ${channel}`)],flags:['Ephemeral']}); } };

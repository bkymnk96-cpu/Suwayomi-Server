const { SlashCommandBuilder, PermissionFlagsBits, ChannelType, EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');
const db = require('../../database/db');
const emojis = require('../../utils/emojis');
const { success } = require('../../utils/embeds');
module.exports = { category:'ticket', data:new SlashCommandBuilder().setName('setup-ticket').setDescription('إنشاء نظام تذاكر احترافي بتجربة تفاعلية').setDefaultMemberPermissions(PermissionFlagsBits.Administrator).addChannelOption(o=>o.setName('channel').setDescription('روم إرسال لوحة التذاكر').addChannelTypes(ChannelType.GuildText).setRequired(true)).addChannelOption(o=>o.setName('category').setDescription('كاتيجوري إنشاء التذاكر').addChannelTypes(ChannelType.GuildCategory).setRequired(true)).addRoleOption(o=>o.setName('support').setDescription('رتبة الدعم').setRequired(true)).addStringOption(o=>o.setName('title').setDescription('عنوان اللوحة')).addStringOption(o=>o.setName('description').setDescription('وصف اللوحة')).addBooleanOption(o=>o.setName('ask_reason').setDescription('طلب سبب فتح التذكرة')), async execute(interaction){ const channel=interaction.options.getChannel('channel'); const category=interaction.options.getChannel('category'); const support=interaction.options.getRole('support'); const title=interaction.options.getString('title') || `${emojis.ticket} نظام التذاكر`; const description=interaction.options.getString('description') || 'اضغط الزر بالأسفل لفتح تذكرة دعم.'; const askReason=interaction.options.getBoolean('ask_reason') || false; const panel={ title, description, color:'#D4AF37', comp_type:'button', comp_label:'فتح تذكرة', comp_emoji:emojis.ticket.toString(), ask_reason:askReason, category_id:category.id, staff_role:support.id }; db.updateTicketSettings(interaction.guild.id,{panel_channel:channel.id,category_id:category.id,staff_role:support.id,support_message:description,panel_data:panel}); const embed=new EmbedBuilder().setColor(0xD4AF37).setTitle(title).setDescription(description).setFooter({text:'ZEUS Tickets',iconURL:interaction.client.user.displayAvatarURL()}).setTimestamp(); const row=new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('ticket_create_btn').setLabel('فتح تذكرة').setEmoji(emojis.ticket.toString()).setStyle(ButtonStyle.Primary)); await channel.send({embeds:[embed],components:[row]}); return interaction.reply({embeds:[success(`${emojis.circlecheck} تم إنشاء نظام التذاكر بنجاح`, `اللوحة: ${channel}\nالفئة: ${category}\nالدعم: ${support}`)],flags:['Ephemeral']}); }};

const { SlashCommandBuilder, PermissionFlagsBits, ChannelType, EmbedBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle } = require('discord.js');
const db = require('../../database/db');
const emojis = require('../../utils/emojis');
const { success, error } = require('../../utils/embeds');
module.exports = { category:'ticket', data:new SlashCommandBuilder().setName('send-preset').setDescription('اختيار إعداد مسبق وإرسال لوحة تذاكر جاهزة').setDefaultMemberPermissions(PermissionFlagsBits.Administrator).addStringOption(o=>o.setName('name').setDescription('اسم الإعداد المسبق').setRequired(true)).addChannelOption(o=>o.setName('channel').setDescription('روم الإرسال').addChannelTypes(ChannelType.GuildText)), async execute(interaction){ const settings=db.getTicketSettings(interaction.guild.id); const preset=(settings.presets||{})[interaction.options.getString('name')]; if(!preset) return interaction.reply({embeds:[error('لم يتم العثور على هذا الإعداد المسبق.')],flags:['Ephemeral']}); const channel=interaction.options.getChannel('channel') || interaction.channel; const embed=new EmbedBuilder().setColor(0xD4AF37).setTitle(preset.title||`${emojis.ticket} نظام التذاكر`).setDescription(preset.description||settings.support_message||'افتح تذكرة من الزر.'); const row=new ActionRowBuilder().addComponents(new ButtonBuilder().setCustomId('ticket_create_btn').setLabel(preset.comp_label||'فتح تذكرة').setEmoji(emojis.ticket.toString()).setStyle(ButtonStyle.Primary)); await channel.send({embeds:[embed],components:[row]}); return interaction.reply({embeds:[success('تم إرسال لوحة التذاكر الجاهزة.')],flags:['Ephemeral']}); }};
